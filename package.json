{
    "name": "bloom_filter_algorithm_visualizer",
    "version": "1.0.0",
    "description": "",
    "scripts": {
        "start": "parcel index.html -p 3000",
        "build": "rm -rf dist && parcel build index.html --dist-dir dist --public-url /bloom_filter_algorithm_visualizer/",
        "predeploy": "rm -rf dist .parcel-cache; npm run build",
        "deploy": "git push origin --delete gh-pages; gh-pages -d dist"
    },
    "keywords": [],
    "author": "",
    "license": "ISC",
    "type": "commonjs",
    "homepage": "https://skalnark.github.io/bloom_filter_algorithm_visualizer",
    "devDependencies": {
        "buffer": "^6.0.3",
        "gh-pages": "^6.3.0",
        "prettier": "^3.6.2",
        "process": "^0.11.10"
    },
    "dependencies": {
        "@faker-js/faker": "^10.0.0",
        "d3": "^7.9.0",
        "parcel": "^2.15.4"
    },
    "prettier": {
        "singleQuote": true,
        "tabWidth": 4
    }
}
